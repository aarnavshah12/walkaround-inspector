# deps / build
node_modules/
.next/
out/
*.tsbuildinfo
.venv/
__pycache__/

# runtime data (captures, chunks, videos, TSA tokens)
data/

# secrets & agent config — never commit (owner directive)
.env
.env.*
.claude/

# local working docs — never commit (owner directive)
CLAUDE.md
PROGRESS.md
walkaround-inspector-plan-final.md
phase6-design.md

# test footage — never commit video files
*.mp4
*.webm
*.mov

# os / editor
.DS_Store
